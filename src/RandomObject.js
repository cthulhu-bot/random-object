import { 
    objDepth, 
    objWidth, 
    getRandom1To10 
} from './util';

const randomObject = (length = util.getRandom1To10(), depth = util.getRandom1To10()) => {
    for (let i = 0; i < rando; i++) {
	console.log(i);
    }
};

module.exports = {
    randomObject,
};
